// Web stub — expo-notifications has no browser implementation. In-app
// notifications still work via the NotificationsScreen Firestore subscription.

export async function registerForPushNotifications() {
  return null;
}

export function attachNotificationListeners() {
  return () => {};
}
